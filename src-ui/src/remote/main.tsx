import React from 'react';
import ReactDOM from 'react-dom/client';
import { RemoteEntry } from './RemoteEntry';
ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><RemoteEntry /></React.StrictMode>);
