import React from 'react';
import ReactDOM from 'react-dom/client';
import { PairEntry } from './PairEntry';

ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><PairEntry /></React.StrictMode>);
